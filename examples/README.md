# Public examples

此目录用于存放可公开、可复现且不包含生产信息的示例资产，例如：

- `schemas/`：脱敏后的 Cube 语义模型；
- `glossary/`：脱敏后的业务术语示例；
- `data/`：许可证明确、体积适当的合成数据。

不要从生产 `state/` 直接复制文件。提交示例前，请移除真实组织名称、人员信息、内部字段、服务器地址、数据库连接和业务数据，并在 Pull Request 中注明数据来源与许可证。
